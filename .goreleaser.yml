version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: kedge
    main: ./cmd/kedge
    binary: kedge
    env:
      - CGO_ENABLED=0
    targets:
      - linux_amd64
      - linux_arm64
      - darwin_amd64
      - darwin_arm64
      - windows_amd64
      - windows_arm64
    ldflags:
      - -s -w
      - -X github.com/LoriKarikari/kedge/internal/version.version={{.Version}}
      - -X github.com/LoriKarikari/kedge/internal/version.commit={{.Commit}}

archives:
  - id: kedge
    formats: ["tar.gz"]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md
      - LICENSE
  - id: kedge-windows
    formats: ["zip"]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md
      - LICENSE
    ids:
      - kedge

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"

release:
  github:
    owner: LoriKarikari
    name: kedge
  prerelease: auto
